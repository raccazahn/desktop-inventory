# Contribution Guidelines

1. Always create a branch: `git checkout -b feature/your-task-name`
2. Never push directly to `main` branch
3. Submit Pull Request when done
4. Wait for Project Lead approval before merging
5. Write clear commit messages
6. Update Project Board column when task status changes
